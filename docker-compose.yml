version: '3.8'

services:
  beat-scrobble-db:
    image: pgvector/pgvector:pg16
    container_name: beat-scrobble-db
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=beatscrobbledb
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    networks:
      - beat-scrobble-net

  beat-scrobble-app:
    image: ghcr.io/saturnx-dev/beat-scrobble:latest
    container_name: beat-scrobble-app
    restart: always
    ports:
      - "4110:4110"
    environment:
      - BEAT_SCROBBLE_DATABASE_URL=postgres://postgres:secret@beat-scrobble-db:5432/beatscrobbledb?sslmode=disable
      - BEAT_SCROBBLE_ALLOWED_HOSTS=*
    volumes:
      - ./config:/etc/beat_scrobble
      - beat_scrobble_cache:/etc/beat_scrobble/image_cache
    depends_on:
      - beat-scrobble-db
    networks:
      - beat-scrobble-net

networks:
  beat-scrobble-net:
    driver: bridge

volumes:
  beat_scrobble_cache:
